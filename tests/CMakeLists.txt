file(GLOB AVEN_TEST_SOURCES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
add_executable(aven_tests ${AVEN_TEST_SOURCES})
target_link_libraries(aven_tests PRIVATE aven_engine)
target_compile_definitions(aven_tests PRIVATE AVEN_SOURCE_DIR="${PROJECT_SOURCE_DIR}")
add_test(NAME aven_tests COMMAND aven_tests)

# A real native module, loaded by test_native.cpp.
add_library(aven_test_module SHARED ${CMAKE_CURRENT_SOURCE_DIR}/native/test_module.c)
target_include_directories(aven_test_module PRIVATE ${PROJECT_SOURCE_DIR}/sdk/include)
set_target_properties(aven_test_module PROPERTIES PREFIX "" C_VISIBILITY_PRESET hidden)
add_dependencies(aven_tests aven_test_module)
target_compile_definitions(aven_tests PRIVATE AVEN_TEST_MODULE="$<TARGET_FILE:aven_test_module>")
if(TARGET aven_example_patrol)
    add_dependencies(aven_tests aven_example_patrol)
    target_compile_definitions(aven_tests PRIVATE AVEN_EXAMPLE_CPP_MODULE="$<TARGET_FILE:aven_example_patrol>")
endif()
